# Core packages for TextAnalysisR Python integration

# spaCy NLP (lemmatize_tokens, spacy_parse_full, NER)
spacy>=3.5.0

# Data handling (reticulate dataframe conversion)
pandas>=1.5.0

# PDF processing
pdfplumber>=0.10.0

# Embeddings and transformer-based features
# (generate_embeddings, generate_content, sentiment analysis)
sentence-transformers>=2.2.0
transformers>=4.30.0
torch>=2.0.0

# Dimensionality reduction + clustering
# (cluster_embeddings with method = "umap_dbscan" / "umap_hdbscan")
umap-learn>=0.5.3
hdbscan>=0.8.29
scikit-learn>=1.2.0
numba>=0.56.0
