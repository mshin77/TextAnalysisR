# Core packages for TextAnalysisR Python integration

# spaCy NLP
spacy>=3.5.0

# Data handling (for reticulate conversion)
pandas>=1.5.0

# PDF processing
pdfplumber>=0.10.0

# Optional: Deep learning features (embeddings, sentiment)
# Uncomment if needed - requires ~500MB+ download
# sentence-transformers>=2.2.0
# transformers>=4.30.0
# torch>=2.0.0
