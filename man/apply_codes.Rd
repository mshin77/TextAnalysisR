% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qualitative_coding.R
\name{apply_codes}
\alias{apply_codes}
\title{Apply a Codebook to Texts}
\usage{
apply_codes(
  texts,
  codebook,
  unit = c("paragraph", "sentence", "document"),
  max_codes = 3,
  provider = c("auto", "openai", "gemini"),
  model = NULL,
  temperature = 0,
  api_key = NULL,
  delay = 1,
  verbose = TRUE
)
}
\arguments{
\item{texts}{Character vector of documents. Names become \code{doc_id}.}

\item{codebook}{Data frame with \code{code} and \code{definition} columns; optional
\code{example} and \code{color}.}

\item{unit}{Unit of analysis: "paragraph" (default, split on blank lines),
"sentence", or "document" (one unit per element of \code{texts}).}

\item{max_codes}{Maximum codes per unit (default 3). Units where no code
fits return one row with \code{code = NA}.}

\item{provider}{AI provider: "auto" (default), "openai", or "gemini".}

\item{model}{Optional model id; provider default when NULL.}

\item{temperature}{Sampling temperature (default 0 for reproducibility).}

\item{api_key}{Optional API key; falls back to the provider env var.}

\item{delay}{Seconds to wait between provider calls (default 1).}

\item{verbose}{Logical; print per-unit progress (default TRUE).}
}
\value{
A tibble with one row per unit-code pair: \code{doc_id}, \code{unit_id},
\code{start}, \code{end} (character offsets of the unit within its document),
\code{code}, \code{confidence}, \code{rationale}. Returns \code{invisible(NULL)} when no
provider key is available.
}
\description{
Suggests codes for each unit of text from a supplied codebook using an AI
provider (OpenAI or Gemini). Documents are split into units (paragraphs by
default) before coding, and each unit receives zero or more codes. Output
is a set of suggestions for human confirmation, not final codes.
}
\seealso{
\code{\link[=code_retest]{code_retest()}} for AI stability; \code{\link[=code_agreement]{code_agreement()}} for
inter-coder reliability; \code{\link[=call_llm_api]{call_llm_api()}} for the direct provider call.
}
\concept{qualitative-coding}
