% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qualitative_coding.R
\name{code_agreement}
\alias{code_agreement}
\title{Inter-Coder Agreement From Code Assignments}
\usage{
code_agreement(
  assignments,
  metrics = c("alpha", "kappa", "ac1", "pabak", "percent"),
  units = c("intersection", "union"),
  by_code = TRUE,
  align = c("grid", "coverage"),
  codebook_authors = NULL
)
}
\arguments{
\item{assignments}{Data frame with \code{doc_id}, \code{code}, and \code{coder} columns.
\code{unit_id} and \code{confidence} are used when present. Coverage additionally
requires \code{start} and \code{end}.}

\item{metrics}{Metrics to report: any of "alpha", "kappa", "ac1", "pabak",
"percent" (all by default). Grid alignment only.}

\item{units}{"intersection" (default, units coded by every coder) or
"union" (uncoded units count as missing). Grid alignment only.}

\item{by_code}{Logical; also report per-code agreement.}

\item{align}{"grid" (default) or "coverage".}

\item{codebook_authors}{Character vector of \code{coder} values that wrote or
revised the codebook. When supplied, metrics are also computed among the
remaining coders alone. Agreement with a coder who shaped the coding frame
reflects that shared calibration, so the two figures answer different
questions. Ignored when \code{align = "coverage"}.}
}
\value{
A list with \code{overall}, \code{by_code} (NULL when \code{by_code} is FALSE),
\code{disagree}, and \code{independent} (NULL unless \code{codebook_authors} is supplied
and at least two other coders remain). For grid alignment these are the
metric, per-code, and disagreement tables; for coverage they hold
per-coder-pair span coverage and the uncovered spans.
}
\description{
Chance-corrected agreement between two or more coders' code assignments.
Reports Krippendorff's alpha and Cohen's/Fleiss' kappa (via irr) plus percent
agreement, Gwet's AC1, and PABAK computed inline. AC1 and PABAK stay stable
under skewed code prevalence, where kappa collapses (the kappa paradox).

With \code{align = "grid"} (default), coders are assumed to share units: rows
pivot on \code{unit_id} when present, else \code{doc_id}. When a coder assigned more
than one code to a unit, the highest-confidence code is kept (first row
when no \code{confidence} column exists); per-code multi-label agreement is in
\code{by_code}. With \code{align = "coverage"}, coders may have different span
boundaries: for each ordered coder pair, the proportion of one coder's
spans that overlap a same-code span from the other is reported instead
(chance-corrected metrics do not apply across unaligned units, so
\code{metrics} and \code{units} are ignored).
}
\seealso{
\code{\link[=apply_codes]{apply_codes()}} to produce assignments; \code{\link[=merge_codes]{merge_codes()}} to combine
coders; \code{\link[=code_retest]{code_retest()}} for AI stability.
}
\concept{qualitative-coding}
