% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text_mining_functions.R
\name{how_to_use}
\alias{how_to_use}
\title{How to Use Functions in TextAnalysisR}
\usage{
how_to_use()
}
\value{
No return value, called for documentation purposes only.
}
\description{
This documentation provides guidance on how to use a set of functions related to:
\itemize{
\item Word-topic probabilities (Beta)
\item Document-topic probabilities (Gamma)
\item Estimated effects of covariates on topic prevalence
\item Network analysis
}

These steps are similar to those demonstrated in the Shiny web app at \code{TextAnalysisR::TextAnalysisR.app()}.
}
\section{Word-Topic Probabilities (Beta)}{

\enumerate{
\item Fit an STM model using \code{stm::stm()}.
\item Extract the beta matrix as a tidy data frame with \code{tidytext::tidy(stm_model, matrix = "beta")}.
\item Use \code{\link{examine_top_terms}} to find top words in each topic.
\item Use \code{\link{plot_topic_term}} to visualize top terms per topic.
}
}

\section{Document-Topic Probabilities (Gamma)}{

\enumerate{
\item Extract gamma from \code{tidytext::tidy(stm_model, matrix = "gamma")}.
\item Use \code{\link{topic_probability_plot}} to visualize topic prevalence.
\item Use \code{\link{topic_probability_table}} to obtain a table of top topics.
}
}

\section{Estimated Effects (Categorical and Continuous Variables)}{

\enumerate{
\item Use \code{stm::estimateEffect()} to model how covariates predict topic proportions.
\item Extract effects using \code{stminsights::get_effects()} and visualize them with custom plotting functions.
}
}

\section{Network Analysis}{

\enumerate{
\item \strong{Hierarchical Clustering}: Use \code{textmineR::CalcHellingerDist()} on \code{stm_model$theta}, then \code{hclust()} and \code{ggdendro::ggdendrogram()} to visualize.
\item \strong{Text Network}: Convert the dfm to a tidy format, use \code{widyr::pairwise_cor()}, filter by correlation, and visualize with \code{igraph} + \code{ggraph}.
\item \strong{Term Frequency Over Time}: Merge dfm counts with document-level variables, group by a continuous variable, and plot changes in term frequency over time with \code{ggplot2}.
}
}

\examples{
# This section provides general guidance; no direct runnable code is required.
}
