% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocessing.R
\name{detect_language}
\alias{detect_language}
\title{Detect Corpus Language From Stopword Overlap}
\usage{
detect_language(
  texts,
  languages = stopwords::stopwords_getlanguages("snowball"),
  sample_n = 200,
  seed = 123
)
}
\arguments{
\item{texts}{Character vector of documents.}

\item{languages}{Candidate language codes (default: all snowball languages).}

\item{sample_n}{Maximum documents to sample (default 200).}

\item{seed}{Seed for sampling.}
}
\value{
A tibble of \code{language} and \code{score} (share of tokens matching that
language's stopwords), ranked best first, or \code{NULL} when no tokens are
found. Ties and low scores mean the corpus language is unclear.
}
\description{
Scores texts against snowball stopword lists and ranks candidate languages by
the share of tokens matching each list. Uses only the \code{stopwords} package, so
no language-detection dependency is added. Accuracy is good for European
languages given a few hundred tokens, and poor for very short texts or
languages absent from snowball; treat the result as a suggestion.
}
\seealso{
\code{\link[=prep_texts]{prep_texts()}} for the \code{stopwords_language} argument this informs.
}
\concept{preprocessing}
