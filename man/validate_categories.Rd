% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pattern_confirmation.R
\name{validate_categories}
\alias{validate_categories}
\title{Confirm Categories With Supervised Learning}
\usage{
validate_categories(
  embeddings,
  categories,
  method = c("knn", "multinom"),
  folds = 5,
  k = 5,
  balance = c("none", "downsample"),
  seed = 123
)
}
\arguments{
\item{embeddings}{Numeric matrix or data frame, one row per document.}

\item{categories}{Category labels, one per row. \code{NA} and \code{-1} are excluded;
place them first with \code{\link[=assign_noise]{assign_noise()}}.}

\item{method}{"knn" (default) or "multinom".}

\item{folds}{Cross-validation folds (default 5), stratified by category.}

\item{k}{Neighbours for \code{method = "knn"} (default 5).}

\item{balance}{"none" (default) or "downsample" to equalize category sizes
within each training fold.}

\item{seed}{Random seed for fold assignment and downsampling.}
}
\value{
A list with \code{overall} (accuracy, macro and weighted F1, counts),
\code{by_category} (support, precision, recall, F1), \code{confusion} (a table of
actual against predicted), and \code{predictions} (per-document actual and
predicted labels).
}
\description{
Tests whether categories are recoverable from the text representation alone.
A classifier maps document embeddings to category labels under stratified
cross-validation. Categories a classifier cannot recover are candidates for
merging or revision.
}
\seealso{
\code{\link[=assign_noise]{assign_noise()}} to place unassigned documents before confirming;
\code{\link[=align_categories]{align_categories()}} to compare human codes against machine clusters;
\code{\link[=fit_embedding_model]{fit_embedding_model()}} to produce the categories being tested.
}
\concept{topic-modeling}
