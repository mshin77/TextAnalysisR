% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qualitative_coding.R
\name{generate_codes}
\alias{generate_codes}
\title{Build a Codebook From Texts}
\usage{
generate_codes(
  texts,
  codebook = NULL,
  unit = c("sentence", "paragraph", "document"),
  order = NULL,
  context_k = Inf,
  stop_after = Inf,
  provider = c("auto", "openai", "gemini"),
  model = NULL,
  temperature = 0,
  api_key = NULL,
  max_tokens = 200,
  delay = 1,
  verbose = TRUE
)
}
\arguments{
\item{texts}{Character vector of documents. Names become \code{doc_id}.}

\item{codebook}{Optional data frame with \code{code} and \code{definition} columns to
start from; \code{NULL} (default) starts empty.}

\item{unit}{Unit of analysis: "sentence" (default), "paragraph" (split on
blank lines), or "document" (one unit per element of \code{texts}).}

\item{order}{Unit ids giving the reading sequence; \code{NULL} (default) reads
in corpus order. Generation is sequential, so the units read first shape
the frame the rest are matched against.}

\item{context_k}{Maximum codes shown in the prompt, most-used first; \code{Inf}
(default) shows all. Capping keeps the reuse decision legible as the
codebook grows.}

\item{stop_after}{Stop once this many consecutive units open no new code;
\code{Inf} (default) reads the whole corpus.}

\item{provider}{AI provider: "auto" (default), "openai", or "gemini".}

\item{model}{Optional model id; provider default when NULL.}

\item{temperature}{Sampling temperature (default 0 for reproducibility).}

\item{api_key}{Optional API key; falls back to the provider env var.}

\item{max_tokens}{Response token cap (default 200).}

\item{delay}{Seconds to wait between provider calls (default 1).}

\item{verbose}{Logical; print per-unit progress (default TRUE).}
}
\value{
A list with \code{codebook} (tibble of \code{code}, \code{definition},
\code{first_unit}) and \code{trace} (one row per unit read: \code{position}, \code{doc_id},
\code{unit_id}, \code{assigned}, \code{opened_new}, \code{n_categories}, \code{status}), one row
per unit read in reading order. The trace shows where categories stopped
accumulating. Returns \code{invisible(NULL)} when no provider key is
available.
}
\description{
Reads one unit of text at a time with the codebook so far in view, and either
reuses a code already in it or opens a new one. The codebook is an output
here, not an input as in \code{\link[=apply_codes]{apply_codes()}}. Supplying \code{codebook} seeds the
reading, which also covers starting from a priori codes or continuing a
stopped run. Categories are proposed for human review, not settled.
}
\seealso{
\code{\link[=apply_codes]{apply_codes()}} to code a corpus once the codebook is settled;
\code{\link[=uncoded_units]{uncoded_units()}} for what a codebook does not reach.
}
\concept{qualitative-coding}
