% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qualitative_coding.R
\name{code_retest}
\alias{code_retest}
\title{AI Coding Retest Stability}
\usage{
code_retest(texts, codebook, n_runs = 2, sample_n = 50, seed = 123, ...)
}
\arguments{
\item{texts}{Character vector of documents. Names become \code{doc_id}.}

\item{codebook}{Data frame with \code{code} and \code{definition} columns.}

\item{n_runs}{Number of coding runs (default 2).}

\item{sample_n}{Maximum number of documents to sample (default 50).}

\item{seed}{Seed for document sampling and the shuffled baseline.}

\item{...}{Passed to \code{\link[=apply_codes]{apply_codes()}} (\code{unit}, \code{max_codes}, \code{provider},
\code{api_key}, \code{delay}, ...).}
}
\value{
A list with \code{summary} (tibble of metric, estimate, n_units,
n_runs) and \code{runs} (all assignments, with a \code{run} column), or
\code{invisible(NULL)} when fewer than two runs complete.
}
\description{
Codes the same sample of texts more than once at identical settings and
reports how often the runs agree, next to a shuffled-label baseline. Low
temperature does not guarantee stable output, so retest agreement is
measured rather than assumed. The baseline shows the agreement expected if
codes were unrelated to the texts; retest agreement should sit well above
it.
}
\seealso{
\code{\link[=apply_codes]{apply_codes()}}; \code{\link[=code_agreement]{code_agreement()}} for human inter-coder
reliability, which this does not replace.
}
\concept{qualitative-coding}
