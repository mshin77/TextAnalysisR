% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qualitative_coding.R
\name{uncoded_units}
\alias{uncoded_units}
\title{Units With No Code Assigned}
\usage{
uncoded_units(assignments, texts)
}
\arguments{
\item{assignments}{Tibble from \code{\link[=apply_codes]{apply_codes()}}, with \code{doc_id}, \code{unit_id},
\code{start}, \code{end}, and \code{code}.}

\item{texts}{Character vector of the documents passed to \code{\link[=apply_codes]{apply_codes()}}.
Names become \code{doc_id}; unnamed vectors are keyed by position.}
}
\value{
A tibble of \code{doc_id}, \code{unit_id}, \code{start}, \code{end}, and \code{unit_text},
one row per uncoded unit, ordered as in \code{assignments}. Zero rows when every
unit received at least one code.
}
\description{
Returns the units \code{\link[=apply_codes]{apply_codes()}} left uncoded, paired with their text.
These units are the ones a codebook does not reach. Reviewing them shows
whether content recurs in the corpus that the coding frame omits, and is the
step that keeps a codebook-constrained analysis from reporting only what the
codebook was built to find.
}
\seealso{
\code{\link[=apply_codes]{apply_codes()}} to produce assignments.
}
\concept{qualitative-coding}
