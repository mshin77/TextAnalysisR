% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/langgraph_workflows.R
\name{generate_topic_labels_langgraph}
\alias{generate_topic_labels_langgraph}
\title{Generate Topic Labels with LLM Assistance}
\usage{
generate_topic_labels_langgraph(
  topic_terms,
  num_topics,
  ollama_model = "llama3",
  ollama_base_url = "http://localhost:11434",
  envname = "textanalysisr-env"
)
}
\arguments{
\item{topic_terms}{List of character vectors, where each vector contains
the top terms for a topic (from STM or other topic model)}

\item{num_topics}{Integer, number of topics}

\item{ollama_model}{Character string, name of Ollama model to use
(default: "llama3")}

\item{ollama_base_url}{Character string, base URL for Ollama API
(default: "http://localhost:11434")}

\item{envname}{Character string, name of Python virtual environment
(default: "langgraph-env")}
}
\value{
List with:
\itemize{
\item success: Logical, TRUE if workflow completed successfully
\item label_candidates: List of label candidate objects for each topic
\item validation_metrics: Validation metrics (if available)
\item error: Error message (if failed)
}
}
\description{
Uses LangGraph workflow to generate multiple label candidates for topics
using local LLM (Ollama). Provides human-in-the-loop review of suggestions.
}
\details{
This function:
\enumerate{
\item Initializes LangGraph Python environment
\item Calls Python workflow to generate label candidates
\item Returns structured results for display in Shiny UI
\item Allows human review and selection of labels
}

The workflow uses a StateGraph with nodes for:
\itemize{
\item Label generation (LLM)
\item Validation (LLM)
\item Conditional revision based on quality metrics
}
}
\examples{
\dontrun{
topic_terms <- list(
  c("education", "student", "learning", "teacher", "school"),
  c("health", "medical", "patient", "doctor", "treatment"),
  c("environment", "climate", "carbon", "emissions", "energy")
)

result <- generate_topic_labels_langgraph(
  topic_terms = topic_terms,
  num_topics = 3,
  ollama_model = "llama3"
)

if (result$success) {
  print(result$label_candidates)
}
}
}
