% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/semantic_analysis.R
\name{sentiment_valence_analysis}
\alias{sentiment_valence_analysis}
\title{Valence-Shifter Sentiment Analysis}
\usage{
sentiment_valence_analysis(
  texts,
  doc_names = NULL,
  neutral_cutoff = 0,
  polarity_dt = NULL
)
}
\arguments{
\item{texts}{Character vector of document texts.}

\item{doc_names}{Optional document identifiers, defaulting to positional names.}

\item{neutral_cutoff}{Absolute score below which a document is called neutral.
Defaults to 0, classifying by sign only.}

\item{polarity_dt}{Optional custom polarity table from
\code{\link[sentimentr:update_polarity_table]{sentimentr::update_polarity_table()}}. The default table scores domain terms
such as "disability" and "intervention" as negative, so a corpus where those
are neutral descriptors needs its own table.}
}
\value{
A list with the same shape as \code{\link[=sentiment_lexicon_analysis]{sentiment_lexicon_analysis()}}:
\describe{
\item{document_sentiment}{Data frame with \code{document}, \code{sentiment_score},
\code{magnitude}, \code{sentences}, and \code{sentiment}}
\item{emotion_scores}{NULL; valence scoring carries no emotion categories}
\item{summary_stats}{Document counts, coverage, polarized-token rate, mean score}
\item{lexicon_used}{"sentimentr"}
\item{feature_type}{"words"}
}
}
\description{
Scores documents with sentimentr, which weights each polarized word by the
negators, amplifiers, de-amplifiers, and adversative conjunctions around it
before aggregating by sentence. Bare lexicon counting reads "not bad at all"
as negative; this returns a positive value.

Scores are unbounded, so compare within a corpus rather than against a fixed
scale. \code{magnitude} is the mean absolute sentence score, computed here rather
than returned by sentimentr.
}
\seealso{
\code{\link[=sentiment_lexicon_analysis]{sentiment_lexicon_analysis()}} for bing/afinn/nrc scoring
}
\concept{sentiment}
