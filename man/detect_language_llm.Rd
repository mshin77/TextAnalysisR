% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocessing.R
\name{detect_language_llm}
\alias{detect_language_llm}
\title{Detect Corpus Language With an LLM}
\usage{
detect_language_llm(
  texts,
  languages,
  provider = c("auto", "openai", "gemini"),
  model = NULL,
  api_key = NULL,
  sample_n = 200,
  seed = 123,
  verbose = TRUE
)
}
\arguments{
\item{texts}{Character vector of documents.}

\item{languages}{Named character vector of candidate languages (name =
display name, value = language code), e.g. \code{c(English = "en", French = "fr")}.}

\item{provider}{One of \code{"auto"}, \code{"openai"}, \code{"gemini"}. \code{"auto"} picks
whichever of \code{OPENAI_API_KEY} / \code{GEMINI_API_KEY} is set, or the key
implied by \code{api_key}'s prefix.}

\item{model}{Model name (default depends on provider).}

\item{api_key}{API key; falls back to the provider's environment variable.}

\item{sample_n}{Maximum documents to sample for the prompt (default 200).}

\item{seed}{Seed for sampling.}

\item{verbose}{Logical, print status messages (default TRUE).}
}
\value{
A one-row tibble with \code{language} (code) and \code{provider}, or \code{NULL}
when no provider/key is available or the response doesn't match a
candidate.
}
\description{
Identifies the dominant language of a text sample by asking an LLM (OpenAI
or Gemini) to pick one name from a candidate list. More accurate than
\code{\link[=detect_language]{detect_language()}} on short texts, mixed-language corpora, or languages
with sparse snowball stopword coverage, at the cost of an API call.
}
\examples{
if (interactive()) {
detect_language_llm(
  c("Bonjour, comment allez-vous?", "Je suis ravi de vous rencontrer."),
  languages = c(English = "en", French = "fr", Spanish = "es"),
  provider = "openai",
  api_key = Sys.getenv("OPENAI_API_KEY")
)
}
}
\seealso{
\code{\link[=detect_language]{detect_language()}} for the local, no-API alternative; \code{\link[=call_llm_api]{call_llm_api()}} for the direct provider call.
}
\concept{ai}
\concept{preprocessing}
