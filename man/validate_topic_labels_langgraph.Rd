% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/langgraph_workflows.R
\name{validate_topic_labels_langgraph}
\alias{validate_topic_labels_langgraph}
\title{Validate User-Selected Topic Labels}
\usage{
validate_topic_labels_langgraph(
  user_labels,
  topic_terms,
  ollama_model = "llama3",
  ollama_base_url = "http://localhost:11434",
  envname = "textanalysisr-env"
)
}
\arguments{
\item{user_labels}{Character vector of user-selected labels for each topic}

\item{topic_terms}{List of character vectors with top terms for each topic}

\item{ollama_model}{Character string, Ollama model name (default: "llama3")}

\item{ollama_base_url}{Character string, Ollama API URL
(default: "http://localhost:11434")}

\item{envname}{Character string, Python virtual environment name
(default: "langgraph-env")}
}
\value{
List with:
\itemize{
\item success: Logical, TRUE if validation completed
\item validation_metrics: List with coherence and distinctiveness scores
\item error: Error message (if failed)
}
}
\description{
Uses LangGraph workflow to validate user-selected topic labels using LLM.
}
\details{
Validation metrics include:
\itemize{
\item coherence_scores: How well labels match term distributions (0-10 scale)
\item distinctiveness_scores: How unique/specific labels are (0-10 scale)
\item overall_quality: Average of coherence and distinctiveness
}
}
\examples{
\dontrun{
user_labels <- c("Education Policy", "Healthcare Services", "Climate Action")
topic_terms <- list(
  c("education", "student", "learning"),
  c("health", "medical", "patient"),
  c("environment", "climate", "carbon")
)

validation <- validate_topic_labels_langgraph(
  user_labels = user_labels,
  topic_terms = topic_terms
)

print(validation$validation_metrics$overall_quality)
}
}
