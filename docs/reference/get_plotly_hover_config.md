# Plotly hover tooltip config

Plotly hover tooltip config

## Usage

``` r
get_plotly_hover_config(bgcolor = "#ffffff", fontcolor = "#0c1f4a")
```

## Arguments

- bgcolor:

  Tooltip background color.

- fontcolor:

  Tooltip text color.

## Value

A list for `plotly::layout(hoverlabel = ...)`.
