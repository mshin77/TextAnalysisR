# Check Docker Deployment

Detects whether the app is running in a Docker container. Docker
deployments have full Python/spaCy capability.

## Usage

``` r
check_docker_deployment()
```

## Value

Logical TRUE if running in Docker
